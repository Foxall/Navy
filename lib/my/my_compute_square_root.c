/*
** EPITECH PROJECT, 2019
** my_compute_square_root
** File description:
** task05 day05
*/

int my_compute_square_root(int nb)
{
    double n = nb;
    double i = 0;
    for (i; nb / i != i; i++) {
        if (i > 32767 || nb > 32767)
            return (0);
    }
    return (i);
}
